extends /layout/forms

block beforehtml
  -
    title = 'Announcer - Staff List'
    description = '適合展示團隊成員、營業時間或其他清單資訊。'

block meta
  meta(property="og:image:height", content="640")
  meta(property="og:image:width", content="1280")
  meta(property="og:image", content="https://scdn.line-apps.com/n/channel_devcenter/img/fx/01_2_restaurant.png")
  meta(property="og:url", content=baseurl + "forms/announcer-template-2.html")

block form
  .form-group.was-validated.my-2
    label(for="vcard-title") 標題 (Title)
    input#vcard-title.form-control.form-control-sm(type="text", required, pattern=".+", v-model="vcard.title")
  
  .form-group.was-validated.my-2
    label(for="vcard-activity") 副標題 (Activity/Tag)
    input#vcard-activity.form-control.form-control-sm(type="text", v-model="vcard.activity")

  .form-group.was-validated.my-2
    label(for="vcard-url") 網站連結 (Website URL)
    input#vcard-url.form-control.form-control-sm(type="url", inputmode="url", v-model.trim="vcard.url")

  .form-group.was-validated.my-2
    label(for="vcard-map") 地圖連結 (Map URL)
    textarea#vcard-map.form-control.form-control-sm(inputmode="url", v-model="vcard.map", rows="2")

  hr.my-3
  h6 成員名單 (List Items)
  
  .card.mb-2(v-for="(person, idx) in vcard.people" :key="idx")
    .card-body.p-2
      h6.card-subtitle.mb-2.text-muted Item {{ idx + 1 }}
      .form-row
        .col
          input.form-control.form-control-sm(placeholder="Name (左側)", v-model="person.name")
        .col
          input.form-control.form-control-sm(placeholder="Time/Info (右側)", v-model="person.time")

  hr.my-4
  h5 獨立分享連結
  .input-group.my-2
    input.form-control.form-control-sm(type="text", readonly, :value="shortcut || ''")
    .input-group-append
      button.btn.btn-outline-secondary(type="button", @click="btnCopy(shortcut)", :disabled="!shortcut") 複製
  small.form-text.text-muted(v-if="shortcut") 此連結已包含目前表單資料，分享或收藏後可直接開啟 LIFF 使用。

block vue-config
  script.
    window.vueConfig.data.vcard = {
      title: '開發團隊名單',
      activity: 'Team Members',
      url: 'https://github.com/tbdavid2019',
      map: 'https://maps.google.com',
      people: [
        { name: 'David', time: 'Frontend' },
        { name: 'Alice', time: 'Design' },
        { name: 'Bob', time: 'Backend' }
      ],
      template: '#{baseurl}cards/announcer-staff.txt',
    }
